spring:
  profiles:
    active:
      -dev
---
spring:
  application:
    name: config-client
  profiles: dev
server:
  port: 8888
username: chuwei
---
spring:
  application:
    name: config-client
  profiles: beta
server:
  port: 9999
