spring:
  profiles:
    active:
      -dev
---
spring:
  application:
    name: config-client
  profiles: dev
server:
  port: 8888
username: zhangpeng
---
spring:
  application:
    name: config-client
  profiles: beta
server:
  port: 9999
