spring:
  profiles:
    active:
      -dev
---
spring:
  application:
    name: config-client
  profiles: dev
server:
  port: 8991
username: chuwei22
---
spring:
  application:
    name: config-client
  profiles: beta
server:
  port: 9999
